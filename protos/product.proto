syntax = "proto3";

package product;

service ProductService {
  rpc GetProducts (Empty) returns (ProductList);
}

message Empty {}

message Product {
  string id = 1;
  string name = 2;
  double price = 3;
  int32 stock = 4;
}

message ProductList {
  repeated Product products = 1;
}